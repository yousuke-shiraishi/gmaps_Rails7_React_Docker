# React + Vite + Material UI (local)

## Commands (inside container via docker-compose)
- The compose service will run `npm install` then `npm run dev` automatically.
- If running locally without Docker:
  ```bash
  npm install
  npm run dev
  ```
